# JOTO TECH 全站 SEO 优化实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 `https://jotoglobal.com` 的全部公开三语言页面建立独立元数据、结构化数据、站点地图、收录控制和中文优先的内容 SEO。

**Architecture:** 使用纯函数 SEO 描述层统一解析“语言 + 路由 + 内容”，由轻量 React 组件同步到文档 `<head>`；公开路由清单使用 JSON 作为浏览器代码与构建脚本的共同数据源。继续保留现有 React SPA 和 Sites Worker 回退，未知路由在客户端呈现本地化 404 并设置 `noindex`。

**Tech Stack:** React 18、TypeScript 5.6、Vite 5、Vitest、Testing Library、Node.js 20、OpenAI Sites

## Global Constraints

- 唯一正式主域名是 `https://jotoglobal.com`。
- 英文页面不使用语言前缀，中文使用 `/zh`，波斯语使用 `/fa`。
- 中文为首要搜索市场，英文和波斯语完整覆盖。
- 不新增运行时依赖或大型 head 管理库。
- 不迁移框架，不在本轮实现预渲染。
- 不改变现有视觉设计，不添加未经确认的资质、客户授权、合作等级或服务承诺。
- 不把 `/preview/` 页面、未知路由、查询参数或锚点写入站点地图。
- 不使用构建时间伪造内容更新时间。
- 保留用户现有的客户 Logo、Hero、样式、测试与 `output/` 改动；每次提交只暂存本任务明确列出的文件。

---

## 文件职责

- `src/seo/public-routes.json`：全部可索引的无语言前缀标准路径，供应用和构建脚本共同使用。
- `src/seo/site.ts`：主域名、语言路径和绝对 URL 的纯函数。
- `src/seo/types.ts`：SEO 描述对象、alternate link 和 JSON-LD 类型。
- `src/seo/content.ts`：固定页面三语言标题与描述；动态页面使用现有内容对象生成。
- `src/seo/descriptor.ts`：识别页面类型并生成完整 SEO 描述。
- `src/seo/structuredData.ts`：构造 Organization、WebSite、WebPage、BreadcrumbList 和 Article JSON-LD。
- `src/seo/SeoHead.tsx`：把 SEO 描述同步到 `<head>`，并在页面切换时清理旧标签。
- `src/pages/NotFoundPage.tsx`：本地化 404 界面。
- `scripts/generate-seo-files.mjs`：读取公共路由清单并生成 `dist/sitemap.xml` 与 `dist/robots.txt`。
- `src/seo/*.test.ts(x)`：URL、描述、head、结构化数据和路由清单测试。
- `scripts/generate-seo-files.test.mjs`：站点地图与 robots 生成测试。

---

### Task 1: 建立公共路由清单与 URL 规范

**Files:**
- Create: `src/seo/public-routes.json`
- Create: `src/seo/site.ts`
- Create: `src/seo/site.test.ts`
- Modify: `tsconfig.json`

**Interfaces:**
- Consumes: `Locale`、`blogArticles`、`solutionCategoryPaths`、`partnerDetails`
- Produces: `SITE_ORIGIN`、`PUBLIC_ROUTES`、`localizedPublicPath()`、`absolutePublicUrl()`、`alternateUrls()`

- [ ] **Step 1: 写 URL 规则的失败测试**

创建 `src/seo/site.test.ts`：

```ts
import { describe, expect, it } from "vitest";
import { blogArticles } from "../content/blog";
import { partnerDetails } from "../content/partners";
import { solutionCategoryPaths } from "../content/solutionCategories";
import routes from "./public-routes.json";
import {
  SITE_ORIGIN,
  absolutePublicUrl,
  alternateUrls,
  localizedPublicPath,
} from "./site";

describe("SEO public URLs", () => {
  it("uses jotoglobal.com as the only production origin", () => {
    expect(SITE_ORIGIN).toBe("https://jotoglobal.com");
  });

  it.each([
    ["en", "/", "/"],
    ["zh-CN", "/", "/zh/"],
    ["fa-IR", "/", "/fa/"],
    ["en", "/about/", "/about"],
    ["zh-CN", "/about/", "/zh/about"],
    ["fa-IR", "/about/", "/fa/about"],
  ] as const)("localizes %s %s", (locale, pathname, expected) => {
    expect(localizedPublicPath(pathname, locale)).toBe(expected);
  });

  it("returns canonical and complete alternate URLs", () => {
    expect(absolutePublicUrl("/solutions/network", "zh-CN")).toBe(
      "https://jotoglobal.com/zh/solutions/network",
    );
    expect(alternateUrls("/solutions/network")).toEqual([
      { hreflang: "en", href: "https://jotoglobal.com/solutions/network" },
      { hreflang: "zh-CN", href: "https://jotoglobal.com/zh/solutions/network" },
      { hreflang: "fa-IR", href: "https://jotoglobal.com/fa/solutions/network" },
      { hreflang: "x-default", href: "https://jotoglobal.com/solutions/network" },
    ]);
  });

  it("keeps the route manifest synchronized with content", () => {
    const expected = [
      "/",
      "/about",
      "/contact",
      "/blog",
      ...blogArticles.map(({ slug }) => `/blog/${slug}`),
      ...solutionCategoryPaths,
      ...partnerDetails.map(({ pathname }) => pathname),
    ].sort();

    expect([...routes].sort()).toEqual(expected);
    expect(new Set(routes).size).toBe(routes.length);
  });
});
```

- [ ] **Step 2: 运行测试并确认失败**

Run: `npm test -- --run src/seo/site.test.ts`

Expected: FAIL，提示 `public-routes.json` 或 `./site` 不存在。

- [ ] **Step 3: 创建唯一公共路由清单**

创建 `src/seo/public-routes.json`，内容必须完整且无重复：

```json
[
  "/",
  "/about",
  "/contact",
  "/blog",
  "/blog/connected-it-and-safeguarding",
  "/blog/enterprise-network-growth",
  "/blog/five-campus-standardisation",
  "/blog/it-operations-in-china",
  "/blog/multi-site-network-rollout",
  "/blog/practical-security-response",
  "/solutions/network",
  "/solutions/security",
  "/solutions/server-storage",
  "/solutions/collaboration",
  "/solutions/safeguarding",
  "/solutions/network/aruba",
  "/solutions/network/cisco",
  "/solutions/network/extreme-networks",
  "/solutions/network/sangfor",
  "/solutions/security/check-point",
  "/solutions/security/fortinet",
  "/solutions/security/knowbe4",
  "/solutions/security/onelogin",
  "/solutions/security/palo-alto-networks",
  "/solutions/security/sangfor",
  "/solutions/server-storage/dell-technologies",
  "/solutions/server-storage/huawei",
  "/solutions/server-storage/inspur",
  "/solutions/collaboration/audiocodes",
  "/solutions/collaboration/cyberdata",
  "/solutions/collaboration/informacast",
  "/solutions/collaboration/vodia",
  "/solutions/safeguarding/hikvision",
  "/solutions/safeguarding/keyking",
  "/solutions/safeguarding/verkada"
]
```

- [ ] **Step 4: 实现 URL 纯函数**

先在 `tsconfig.json` 的 `compilerOptions` 中增加：

```json
"resolveJsonModule": true
```

创建 `src/seo/site.ts`：

```ts
import type { Locale } from "../i18n/routing";
import routeManifest from "./public-routes.json";

export const SITE_ORIGIN = "https://jotoglobal.com";
export const PUBLIC_ROUTES = routeManifest as readonly string[];

const prefixes: Record<Locale, string> = {
  en: "",
  "zh-CN": "/zh",
  "fa-IR": "/fa",
};

function normalizePublicPath(pathname: string): string {
  const pathOnly = pathname.split(/[?#]/, 1)[0] || "/";
  const withLeadingSlash = pathOnly.startsWith("/") ? pathOnly : `/${pathOnly}`;
  const normalized = withLeadingSlash.replace(/\/+$/, "");
  return normalized || "/";
}

export function localizedPublicPath(pathname: string, locale: Locale): string {
  const normalized = normalizePublicPath(pathname);
  const prefix = prefixes[locale];
  if (normalized === "/") return prefix ? `${prefix}/` : "/";
  return `${prefix}${normalized}`;
}

export function absolutePublicUrl(pathname: string, locale: Locale): string {
  return `${SITE_ORIGIN}${localizedPublicPath(pathname, locale)}`;
}

export function alternateUrls(pathname: string) {
  return [
    { hreflang: "en", href: absolutePublicUrl(pathname, "en") },
    { hreflang: "zh-CN", href: absolutePublicUrl(pathname, "zh-CN") },
    { hreflang: "fa-IR", href: absolutePublicUrl(pathname, "fa-IR") },
    { hreflang: "x-default", href: absolutePublicUrl(pathname, "en") },
  ] as const;
}
```

- [ ] **Step 5: 运行测试并确认通过**

Run: `npm test -- --run src/seo/site.test.ts`

Expected: PASS，4 组 URL/清单测试全部通过。

- [ ] **Step 6: 提交**

```bash
git add src/seo/public-routes.json src/seo/site.ts src/seo/site.test.ts tsconfig.json
git commit -m "feat: define canonical SEO routes"
```

---

### Task 2: 生成中文优先的页面 SEO 描述

**Files:**
- Create: `src/seo/types.ts`
- Create: `src/seo/content.ts`
- Create: `src/seo/descriptor.ts`
- Create: `src/seo/descriptor.test.ts`

**Interfaces:**
- Consumes: `getBlogArticle()`、`getSolutionCategoryDetail()`、`getPartnerDetail()`、`localizePartnerDetail()`
- Produces: `SeoDescriptor`、`buildSeoDescriptor(locale, pathname): SeoDescriptor`

- [ ] **Step 1: 写页面识别和文案的失败测试**

创建 `src/seo/descriptor.test.ts`，至少覆盖以下断言：

```ts
import { describe, expect, it } from "vitest";
import { buildSeoDescriptor } from "./descriptor";

describe("page SEO descriptors", () => {
  it("prioritizes Chinese enterprise IT intent on the home page", () => {
    const seo = buildSeoDescriptor("zh-CN", "/");
    expect(seo.title).toBe("企业 IT 解决方案与系统集成服务 | JOTO TECH");
    expect(seo.description).toContain("企业网络、网络安全、服务器与存储");
    expect(seo.canonicalUrl).toBe("https://jotoglobal.com/zh/");
    expect(seo.robots).toBe("index, follow");
  });

  it("uses article content for localized article metadata", () => {
    const seo = buildSeoDescriptor(
      "zh-CN",
      "/blog/enterprise-network-growth",
    );
    expect(seo.title).toBe("构建能够随业务增长的企业网络 | JOTO TECH");
    expect(seo.description).toBe(
      "可靠的网络应从业务优先级、站点现实和运维责任出发，而不是从设备清单出发。",
    );
    expect(seo.openGraph.type).toBe("article");
  });

  it("combines partner brand, solution and JOTO delivery", () => {
    const seo = buildSeoDescriptor("zh-CN", "/solutions/network/cisco");
    expect(seo.title).toBe("Cisco 企业网络解决方案、部署与运维 | JOTO TECH");
    expect(seo.description).toContain("Cisco");
    expect(seo.description).toContain("规划、部署");
  });

  it("marks preview and unknown paths as non-indexable", () => {
    expect(
      buildSeoDescriptor("en", "/preview/customer-logo-wall").robots,
    ).toBe("noindex, nofollow");
    const missing = buildSeoDescriptor("fa-IR", "/missing");
    expect(missing.status).toBe("not-found");
    expect(missing.canonicalUrl).toBeUndefined();
    expect(missing.alternates).toEqual([]);
  });
});
```

- [ ] **Step 2: 运行测试并确认失败**

Run: `npm test -- --run src/seo/descriptor.test.ts`

Expected: FAIL，提示 SEO 类型和描述生成器不存在。

- [ ] **Step 3: 定义稳定的数据接口**

创建 `src/seo/types.ts`：

```ts
export type SeoStatus = "public" | "preview" | "not-found";
export type OpenGraphType = "website" | "article";

export interface SeoAlternate {
  hreflang: "en" | "zh-CN" | "fa-IR" | "x-default";
  href: string;
}

export interface JsonLdNode {
  "@context"?: "https://schema.org";
  "@type": string | string[];
  "@id"?: string;
  [key: string]: unknown;
}

export interface SeoDescriptor {
  status: SeoStatus;
  title: string;
  description: string;
  robots: "index, follow" | "noindex, nofollow";
  canonicalUrl?: string;
  alternates: readonly SeoAlternate[];
  openGraph: {
    type: OpenGraphType;
    title: string;
    description: string;
    url?: string;
    locale: string;
    alternateLocales: readonly string[];
    image: string;
    imageAlt: string;
    imageWidth: number;
    imageHeight: number;
  };
  structuredData: JsonLdNode[];
}
```

- [ ] **Step 4: 创建固定页面三语言文案**

创建 `src/seo/content.ts`。固定页面使用以下明确文案；动态页面从现有文章、解决方案和厂商内容生成：

```ts
import type { Locale } from "../i18n/routing";

export type FixedPageKey = "home" | "about" | "contact" | "blog";

export const fixedSeoCopy: Record<
  Locale,
  Record<FixedPageKey, { title: string; description: string }>
> = {
  en: {
    home: {
      title: "Enterprise IT Solutions & Systems Integration | JOTO TECH",
      description: "JOTO TECH designs, integrates and supports enterprise networks, cybersecurity, servers, storage, collaboration and physical security in China and global markets.",
    },
    about: {
      title: "About JOTO TECH | Enterprise IT Integrator in China",
      description: "Founded in Shanghai in 2010, JOTO TECH plans, integrates and supports complex enterprise IT and physical-security environments.",
    },
    contact: {
      title: "Contact JOTO TECH | Discuss Your Enterprise IT Project",
      description: "Talk to JOTO TECH about enterprise networks, security, data centers, collaboration, physical security, multi-site deployment or managed IT support.",
    },
    blog: {
      title: "Enterprise IT Insights & Project Experience | JOTO TECH",
      description: "Practical insights on enterprise networks, cybersecurity, IT operations, multi-site delivery and connected physical security from JOTO TECH.",
    },
  },
  "zh-CN": {
    home: {
      title: "企业 IT 解决方案与系统集成服务 | JOTO TECH",
      description: "JOTO TECH 提供企业网络、网络安全、服务器与存储、协作通信及物理安防解决方案，覆盖 IT 规划、系统集成、项目实施与持续运维。",
    },
    about: {
      title: "关于 JOTO TECH | 上海企业 IT 系统集成服务商",
      description: "JOTO TECH 于 2010 年创立于上海，为企业提供 IT 规划、系统集成、项目交付与持续运维服务。",
    },
    contact: {
      title: "联系 JOTO TECH | 咨询企业 IT 项目与解决方案",
      description: "咨询企业网络、网络安全、服务器与存储、协作通信、物理安防、多站点部署及 IT 运维服务。",
    },
    blog: {
      title: "企业 IT 洞察、技术实践与项目经验 | JOTO TECH",
      description: "阅读 JOTO TECH 关于企业网络、网络安全、IT 运维、多站点项目交付与物理安防集成的实践洞察。",
    },
  },
  "fa-IR": {
    home: {
      title: "راهکارهای فناوری اطلاعات و یکپارچه‌سازی سازمانی | JOTO TECH",
      description: "JOTO TECH شبکه، امنیت سایبری، سرور، ذخیره‌سازی، ارتباطات یکپارچه و امنیت فیزیکی سازمانی را طراحی، اجرا و پشتیبانی می‌کند.",
    },
    about: {
      title: "درباره JOTO TECH | یکپارچه‌ساز فناوری اطلاعات سازمانی",
      description: "JOTO TECH از سال ۲۰۱۰ محیط‌های پیچیده فناوری اطلاعات و امنیت فیزیکی سازمانی را طراحی، یکپارچه و پشتیبانی می‌کند.",
    },
    contact: {
      title: "تماس با JOTO TECH | مشاوره پروژه فناوری اطلاعات سازمانی",
      description: "برای شبکه، امنیت، مرکز داده، ارتباطات، امنیت فیزیکی، استقرار چندسایتی و پشتیبانی مدیریت‌شده با JOTO TECH گفتگو کنید.",
    },
    blog: {
      title: "دیدگاه‌ها و تجربه پروژه‌های فناوری اطلاعات | JOTO TECH",
      description: "دیدگاه‌های عملی JOTO TECH درباره شبکه سازمانی، امنیت سایبری، عملیات فناوری اطلاعات و تحویل پروژه‌های چندسایتی.",
    },
  },
};
```

- [ ] **Step 5: 实现页面识别和动态标题规则**

在 `src/seo/descriptor.ts` 中按以下顺序解析：

1. `/preview/` → `preview`
2. `/`、`/about`、`/contact`、`/blog` → 固定页面
3. `/blog/:slug` 且文章存在 → Article
4. 有效解决方案分类 → 分类页
5. 有效厂商详情 → 厂商页
6. 其他路径 → `not-found`

动态标题使用以下规则：

```ts
function articleTitle(title: string) {
  return `${title} | JOTO TECH`;
}

function categoryTitle(locale: Locale, title: string) {
  if (locale === "zh-CN") return `${title}解决方案、系统集成与运维 | JOTO TECH`;
  if (locale === "fa-IR") return `راهکار، یکپارچه‌سازی و پشتیبانی ${title} | JOTO TECH`;
  return `${title} Solutions, Integration & Support | JOTO TECH`;
}

function partnerTitle(locale: Locale, partner: string, solution: string) {
  if (locale === "zh-CN") return `${partner} 企业${solution}解决方案、部署与运维 | JOTO TECH`;
  if (locale === "fa-IR") return `راهکار، استقرار و پشتیبانی ${partner} برای ${solution} | JOTO TECH`;
  return `${partner} ${solution} Solutions, Deployment & Support | JOTO TECH`;
}
```

对于 `Cisco + 网络`，中文结果必须规范为：

```txt
Cisco 企业网络解决方案、部署与运维 | JOTO TECH
```

公开页面使用 `absolutePublicUrl()` 和 `alternateUrls()`；preview 和 not-found 不输出 canonical、alternate 或结构化数据。默认分享图使用 `new URL(jotoLogo, SITE_ORIGIN).href`，尺寸为现有 Logo 的 `752 × 256`。

- [ ] **Step 6: 运行描述测试**

Run: `npm test -- --run src/seo/descriptor.test.ts src/seo/site.test.ts`

Expected: PASS。

- [ ] **Step 7: 提交**

```bash
git add src/seo/types.ts src/seo/content.ts src/seo/descriptor.ts src/seo/descriptor.test.ts
git commit -m "feat: add localized page SEO descriptions"
```

---

### Task 3: 把 SEO 描述安全同步到文档 head

**Files:**
- Create: `src/seo/SeoHead.tsx`
- Create: `src/seo/SeoHead.test.tsx`
- Modify: `src/i18n/I18nProvider.tsx`
- Modify: `src/i18n/I18nProvider.test.tsx`
- Modify: `src/App.tsx`

**Interfaces:**
- Consumes: `SeoDescriptor`、`buildSeoDescriptor()`
- Produces: `<SeoHead descriptor={descriptor} />`

- [ ] **Step 1: 写 head 渲染失败测试**

测试需渲染公开页，再 rerender 为预览页，确认旧 canonical 和 alternate 被清理：

```tsx
const { rerender } = render(<SeoHead descriptor={publicSeo} />);
expect(document.title).toBe(publicSeo.title);
expect(document.head.querySelector('link[rel="canonical"]')).toHaveAttribute(
  "href",
  publicSeo.canonicalUrl,
);
expect(document.head.querySelectorAll('link[rel="alternate"]')).toHaveLength(4);
expect(document.head.querySelector('meta[property="og:url"]')).toHaveAttribute(
  "content",
  publicSeo.canonicalUrl,
);

rerender(<SeoHead descriptor={previewSeo} />);
expect(document.head.querySelector('meta[name="robots"]')).toHaveAttribute(
  "content",
  "noindex, nofollow",
);
expect(document.head.querySelector('link[rel="canonical"]')).toBeNull();
expect(document.head.querySelectorAll('link[rel="alternate"]')).toHaveLength(0);
```

- [ ] **Step 2: 运行测试并确认失败**

Run: `npm test -- --run src/seo/SeoHead.test.tsx`

Expected: FAIL，提示 `SeoHead` 不存在。

- [ ] **Step 3: 实现无依赖 head 管理**

`SeoHead.tsx` 使用带 `data-joto-seo` 标记的标签。每次 effect：

1. 更新 `document.title`
2. 删除所有旧 `[data-joto-seo]`
3. 写入 description、robots、Open Graph、Twitter
4. 仅当存在 canonical 时写 canonical
5. 遍历 alternates 写 `<link rel="alternate" hreflang="...">`
6. 组件卸载时删除动态标签

Open Graph 必须输出：

```txt
og:type
og:site_name = JOTO TECH
og:title
og:description
og:url（公开页）
og:locale
og:locale:alternate
og:image
og:image:alt
og:image:width
og:image:height
```

Twitter 必须输出：

```txt
twitter:card = summary_large_image
twitter:title
twitter:description
twitter:image
```

- [ ] **Step 4: 移除 I18nProvider 中重复的页面元数据职责**

保留：

```ts
useEffect(() => {
  document.documentElement.lang = locale;
  document.documentElement.dir = direction;
}, [direction, locale]);
```

删除 `I18nProvider` 对 `document.title` 和 `meta[name="description"]` 的直接修改，并更新其测试，使测试只验证 `lang`、`dir` 和语言跳转。

- [ ] **Step 5: 在 App 顶层挂载唯一 SeoHead**

在 `App.tsx` 中：

```tsx
const seo = buildSeoDescriptor(locale, pathname);
```

将页面内容解析成单一 `page` 变量，再统一返回：

```tsx
return (
  <>
    <SeoHead descriptor={seo} />
    {page}
  </>
);
```

预览页、博客文章、解决方案分类、厂商详情和首页都必须经过同一个返回出口，避免任何公开路由漏掉 SEO。

- [ ] **Step 6: 运行相关测试**

Run: `npm test -- --run src/seo/SeoHead.test.tsx src/i18n/I18nProvider.test.tsx src/App.test.tsx`

Expected: PASS。

- [ ] **Step 7: 提交**

```bash
git add src/seo/SeoHead.tsx src/seo/SeoHead.test.tsx src/i18n/I18nProvider.tsx src/i18n/I18nProvider.test.tsx src/App.tsx
git commit -m "feat: render route-level SEO metadata"
```

---

### Task 4: 增加结构化数据

**Files:**
- Create: `src/seo/structuredData.ts`
- Create: `src/seo/structuredData.test.ts`
- Modify: `src/seo/descriptor.ts`
- Modify: `src/seo/SeoHead.tsx`
- Modify: `src/seo/SeoHead.test.tsx`

**Interfaces:**
- Consumes: 页面语言、标准路由、文章/解决方案/厂商内容
- Produces: `buildStructuredData(input): JsonLdNode[]`

- [ ] **Step 1: 写 JSON-LD 失败测试**

覆盖：

```ts
const home = buildSeoDescriptor("zh-CN", "/");
expect(home.structuredData.map((node) => node["@type"])).toEqual(
  expect.arrayContaining(["Organization", "WebSite", "WebPage"]),
);

const article = buildSeoDescriptor("en", "/blog/enterprise-network-growth");
expect(article.structuredData.map((node) => node["@type"])).toEqual(
  expect.arrayContaining(["Article", "BreadcrumbList"]),
);
expect(article.structuredData.find((node) => node["@type"] === "Article")).toMatchObject({
  headline: "Building an Enterprise Network That Can Grow With the Business",
  datePublished: "2026-07-22",
  inLanguage: "en",
});
```

同时验证每个节点可被 `JSON.stringify()`，且 preview/not-found 的数组为空。

- [ ] **Step 2: 运行测试并确认失败**

Run: `npm test -- --run src/seo/structuredData.test.ts`

Expected: FAIL。

- [ ] **Step 3: 实现稳定实体 ID 和 Schema 图**

`structuredData.ts` 使用：

```ts
const organizationId = `${SITE_ORIGIN}/#organization`;
const websiteId = `${SITE_ORIGIN}/#website`;
```

Organization 只写站内已确认字段：

```ts
{
  "@type": "Organization",
  "@id": organizationId,
  name: "JOTO TECH",
  legalName: "JOTO Tech (SH) Co., Ltd.",
  url: `${SITE_ORIGIN}/`,
  logo: absoluteLogoUrl,
  email: "sales@jototech.cn",
  foundingDate: "2010",
}
```

首页增加 WebSite 和 WebPage；关于页使用 `AboutPage`；联系页使用 `ContactPage`；普通列表、解决方案和厂商页使用 `WebPage`。内部页增加 `BreadcrumbList`。Article 使用现有 `publishedAt`，不输出不存在的修改日期。

- [ ] **Step 4: 在 SeoHead 写入单个 @graph 脚本**

公开页面输出：

```html
<script type="application/ld+json" data-joto-seo>
{"@context":"https://schema.org","@graph":[...]}
</script>
```

用 `JSON.stringify()` 设置 `textContent`，不使用 `innerHTML`。preview/not-found 不创建脚本。

- [ ] **Step 5: 运行结构化数据与 head 测试**

Run: `npm test -- --run src/seo/structuredData.test.ts src/seo/SeoHead.test.tsx src/seo/descriptor.test.ts`

Expected: PASS。

- [ ] **Step 6: 提交**

```bash
git add src/seo/structuredData.ts src/seo/structuredData.test.ts src/seo/descriptor.ts src/seo/SeoHead.tsx src/seo/SeoHead.test.tsx
git commit -m "feat: add schema.org structured data"
```

---

### Task 5: 添加本地化 404 和收录控制

**Files:**
- Create: `src/pages/NotFoundPage.tsx`
- Create: `src/pages/NotFoundPage.test.tsx`
- Modify: `src/App.tsx`
- Modify: `src/App.test.tsx`

**Interfaces:**
- Consumes: `useI18n()`、`localizedHref()`
- Produces: `<NotFoundPage />`

- [ ] **Step 1: 写 404 失败测试**

测试 `/zh/not-a-page`：

```tsx
expect(screen.getByRole("heading", { level: 1 })).toHaveTextContent("页面未找到");
expect(screen.getByRole("link", { name: "返回首页" })).toHaveAttribute(
  "href",
  "/zh/",
);
expect(document.querySelector('meta[name="robots"]')).toHaveAttribute(
  "content",
  "noindex, nofollow",
);
expect(document.querySelector('link[rel="canonical"]')).toBeNull();
```

- [ ] **Step 2: 运行测试并确认失败**

Run: `npm test -- --run src/pages/NotFoundPage.test.tsx`

Expected: FAIL，当前未知路径会显示首页。

- [ ] **Step 3: 实现品牌化 404**

404 文案：

| Locale | Eyebrow | Title | Description |
|---|---|---|---|
| en | JOTO TECH / 404 | Page not found. | The page may have moved or the address may be incomplete. |
| zh-CN | JOTO TECH / 404 | 页面未找到。 | 页面可能已移动，或当前地址不完整。 |
| fa-IR | JOTO TECH / 404 | صفحه پیدا نشد. | ممکن است صفحه جابه‌جا شده باشد یا نشانی کامل نباشد. |

提供四个真实链接：首页、解决方案、最新资讯、联系我们；所有链接保留当前语言。使用现有 `Header`/`SiteFooter` 或内部页面的深色风格，不新增布局体系。

- [ ] **Step 4: 让 App 的最终分支返回 NotFoundPage**

只有 `pathname === "/"` 才返回首页；其他未匹配路径一律返回 `<NotFoundPage />`。

- [ ] **Step 5: 运行 404 与应用测试**

Run: `npm test -- --run src/pages/NotFoundPage.test.tsx src/App.test.tsx src/seo/descriptor.test.ts`

Expected: PASS。

- [ ] **Step 6: 提交**

```bash
git add src/pages/NotFoundPage.tsx src/pages/NotFoundPage.test.tsx src/App.tsx src/App.test.tsx
git commit -m "feat: add localized non-indexable 404 page"
```

---

### Task 6: 生成 sitemap.xml 与 robots.txt

**Files:**
- Create: `scripts/generate-seo-files.mjs`
- Create: `scripts/generate-seo-files.test.mjs`
- Modify: `package.json`
- Modify: `index.html`

**Interfaces:**
- Consumes: `src/seo/public-routes.json`
- Produces: `dist/sitemap.xml`、`dist/robots.txt`

- [ ] **Step 1: 写构建文件失败测试**

用 Node 内置 `node:test` 在临时目录调用：

```js
const result = await generateSeoFiles(outputDirectory);
const sitemap = await readFile(result.sitemapPath, "utf8");
const robots = await readFile(result.robotsPath, "utf8");

assert.match(sitemap, /<loc>https:\/\/jotoglobal\.com\/zh\/about<\/loc>/);
assert.match(sitemap, /hreflang="fa-IR"/);
assert.doesNotMatch(sitemap, /preview/);
assert.equal(
  robots,
  [
    "User-agent: *",
    "Allow: /",
    "Disallow: /preview/",
    "Disallow: /zh/preview/",
    "Disallow: /fa/preview/",
    "",
    "Sitemap: https://jotoglobal.com/sitemap.xml",
    "",
  ].join("\n"),
);
```

- [ ] **Step 2: 运行测试并确认失败**

Run: `node --test scripts/generate-seo-files.test.mjs`

Expected: FAIL，生成器不存在。

- [ ] **Step 3: 实现安全的 XML 生成**

`generate-seo-files.mjs`：

- 使用 `fs/promises` 读取 `src/seo/public-routes.json`。
- 对每个标准路径生成 en、zh-CN、fa-IR 三个 `<url>`。
- 每个 `<url>` 均输出 en、zh-CN、fa-IR 和 x-default 的 `xhtml:link`。
- 根路径正确输出 `/`、`/zh/`、`/fa/`。
- 使用 XML 转义函数处理 `& < > " '`。
- 不输出 `lastmod`、`changefreq` 或 `priority`。
- 导出 `generateSeoFiles(outputDirectory)`，并在脚本直接运行时写入 `dist/`。

- [ ] **Step 4: 串联生产构建**

将 `package.json` 更新为：

```json
"build": "tsc --noEmit && vite build && node scripts/generate-seo-files.mjs",
"build:github-pages": "tsc --noEmit && vite build --mode github-pages && node scripts/generate-seo-files.mjs",
"build:sites": "npm run build && node scripts/prepare-sites-build.mjs",
"test:seo-files": "node --test scripts/generate-seo-files.test.mjs"
```

生成器始终写入当前项目 `dist/`，因此 GitHub Pages 和 Sites 构建都会携带相同文件。

- [ ] **Step 5: 补充 index.html 的安全回退**

`index.html` 保留英文默认标题和 description，并增加：

```html
<meta name="robots" content="index, follow" />
<meta property="og:site_name" content="JOTO TECH" />
<meta property="og:type" content="website" />
<meta name="twitter:card" content="summary_large_image" />
```

这些字段用于 JavaScript 执行前的基础回退；运行时 `SeoHead` 会替换为页面级内容。不要在 `index.html` 写固定 canonical，避免深层路由错误继承首页 canonical。

- [ ] **Step 6: 运行测试和构建**

Run:

```bash
npm run test:seo-files
npm run build
```

Expected:

- Node 测试 PASS。
- TypeScript 和 Vite 构建 PASS。
- `dist/sitemap.xml` 包含 105 个 `<url>`（35 个标准路径 × 3 种语言）。
- `dist/robots.txt` 指向正式 sitemap。
- `dist/server/index.js` 尚未生成；它由 `build:sites` 负责。

- [ ] **Step 7: 提交**

```bash
git add scripts/generate-seo-files.mjs scripts/generate-seo-files.test.mjs package.json index.html
git commit -m "feat: generate sitemap and robots files"
```

---

### Task 7: 优化中文可见摘要并检查语义结构

**Files:**
- Modify: `src/i18n/translations.ts`
- Modify: `src/content/blog.ts`
- Modify: `src/App.test.tsx`
- Modify: `src/pages/BlogPage.test.tsx`

**Interfaces:**
- Consumes: 现有翻译字典和博客页面文案
- Produces: 中文首页与博客列表的搜索意图清晰摘要

- [ ] **Step 1: 写中文可见文案失败测试**

首页中文 Hero 摘要预期：

```txt
自 2010 年起，JOTO TECH 为企业提供网络、安全、服务器与存储、协作通信及物理安防解决方案，覆盖 IT 规划、系统集成与持续运维。
```

中文博客列表摘要预期：

```txt
来自 JOTO 项目与运维实践的企业网络、网络安全、IT 运营、多站点交付和物理安防集成洞察。
```

分别在 `App.test.tsx` 和 `BlogPage.test.tsx` 断言页面可见这些完整文本。

- [ ] **Step 2: 运行测试并确认失败**

Run: `npm test -- --run src/App.test.tsx src/pages/BlogPage.test.tsx`

Expected: FAIL，仍显示旧摘要。

- [ ] **Step 3: 更新中文内容**

在 `translations.ts` 中只替换英文 Hero description 对应的中文值，不改变英文源文和波斯语：

```ts
"Enterprise networks, security, data centers, collaboration and physical safeguarding — designed, built and supported for the world's most demanding companies since 2010.":
  "自 2010 年起，JOTO TECH 为企业提供网络、安全、服务器与存储、协作通信及物理安防解决方案，覆盖 IT 规划、系统集成与持续运维。",
```

在 `blogPageCopy["zh-CN"].description` 中写入：

```ts
"来自 JOTO 项目与运维实践的企业网络、网络安全、IT 运营、多站点交付和物理安防集成洞察。"
```

- [ ] **Step 4: 检查标题层级与图片语义**

用以下扫描确认：

```bash
rg -n "<h1|<h2|<h3|<img|alt=" src/pages src/components
```

验收规则：

- 每个公开页面只有一个 `<h1>`。
- 可索引主图使用准确 alt。
- Logo 轮播的重复副本继续使用空 alt。
- 预览页不影响公开页面语义检查。

如果扫描发现公开页面的真实问题，只做最小修复，并为该修复添加对应页面测试；不要调整视觉样式或重构无关组件。

- [ ] **Step 5: 运行文案和页面测试**

Run: `npm test -- --run src/App.test.tsx src/pages/BlogPage.test.tsx`

Expected: PASS。

- [ ] **Step 6: 提交**

```bash
git add src/i18n/translations.ts src/content/blog.ts src/App.test.tsx src/pages/BlogPage.test.tsx
git commit -m "content: clarify Chinese enterprise IT search intent"
```

---

### Task 8: 全量验证、Sites 构建与生产部署

**Files:**
- Modify only if verification reveals an in-scope defect.
- Inspect: `.openai/hosting.json`
- Inspect: `dist/**`

**Interfaces:**
- Consumes: 前七个任务的全部产物
- Produces: 通过测试的 Sites 生产版本

- [ ] **Step 1: 运行全量自动测试**

Run:

```bash
npm test -- --run
npm run test:seo-files
```

Expected: 全部 Vitest 和 Node 测试 PASS。若失败，先判断是否由本任务造成；不得覆盖用户现有改动来消除失败。

- [ ] **Step 2: 运行 Sites 生产构建**

Run:

```bash
npm run build:sites
```

Expected:

- TypeScript PASS。
- Vite PASS。
- `dist/sitemap.xml` 与 `dist/robots.txt` 存在。
- `dist/server/index.js` 存在。

- [ ] **Step 3: 静态检查生产产物**

Run:

```bash
test -f dist/sitemap.xml
test -f dist/robots.txt
test -f dist/server/index.js
rg -n "jotoglobal.com|hreflang|preview" dist/sitemap.xml dist/robots.txt
```

Expected:

- sitemap 只使用 `https://jotoglobal.com`。
- 包含 `en`、`zh-CN`、`fa-IR`、`x-default`。
- sitemap 不含 preview。
- robots 包含三个 preview Disallow 与正式 Sitemap。

- [ ] **Step 4: 浏览器检查代表性路由**

启动 `npm run dev`，检查：

```txt
/
/zh/
/fa/
/zh/about
/zh/blog/enterprise-network-growth
/zh/solutions/network
/zh/solutions/network/cisco
/preview/customer-logo-wall
/zh/not-a-real-page
```

每个公开路由检查 title、description、canonical、四个 alternate、Open Graph、Twitter 和 JSON-LD。预览页与未知路由检查 `noindex, nofollow` 且无 canonical。确认控制台无错误、页面无横向溢出。

- [ ] **Step 5: 复核工作树与提交最终修复**

Run:

```bash
git diff --check
git status --short
```

只暂存 SEO 实施文件。若验证阶段没有产生新修复，则不创建空提交。

- [ ] **Step 6: 按 Sites 工作流保存并部署**

执行前完整读取 `sites:sites-building` 与 `sites:sites-hosting` 技能。读取 `.openai/hosting.json`，复用精确的：

```txt
project_id = appgprj_6a6181c317588191bf96fe984963220b
```

要求：

- 不创建第二个 Site。
- 推送与当前源代码完全一致的状态。
- 先保存版本，再部署该已保存版本。
- 初始部署状态非终态时继续检查，直到成功或返回明确错误。
- 记录最终生产 URL 和部署状态。

- [ ] **Step 7: 最终交付**

向用户报告：

- 新增的页面级 SEO 能力。
- 覆盖的公开路由与语言数。
- 测试和构建结果。
- 生产部署 URL。
- 现有用户改动未被纳入 SEO 提交。
- 搜索引擎站长平台提交属于上线后的独立操作，未在本轮代为执行。
