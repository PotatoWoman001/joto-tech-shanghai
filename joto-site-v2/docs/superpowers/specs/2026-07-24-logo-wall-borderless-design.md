# Logo Wall 无边界设计

## 目标

降低暗色玻璃 Logo Wall 与上下内容之间的硬边界感，让它更自然地融入页面背景。

## 已确认方案

- 移除 `.customer-logo-wall__ribbon` 当前通过 `box-shadow` 绘制的上下两条内描边。
- 保留深色玻璃背景、中央绿色微光和左右渐隐。
- 不修改 Logo 的尺寸、对比度处理、滚动速度、悬停暂停或移动端行为。

## 验收标准

- Logo Wall 上下边缘不再出现连续细线。
- 42 个 Logo 继续正常显示。
- 桌面端和移动端不产生横向页面溢出。
