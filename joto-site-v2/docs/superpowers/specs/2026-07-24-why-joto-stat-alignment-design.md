# Why JOTO 信息格对齐设计

## 问题

四个信息格的说明文字使用固定上边距。由于上方数值长度和字号不同，说明文字落在不同高度，形成明显错位。

## 已确认方案

- 首页 `About` 区域和 About 详情页的信息格统一使用纵向 Flex 布局。
- 数值保持卡片顶部位置不变。
- 说明文字使用 `margin-top: auto` 锚定到卡片底部，并保留原有最小间距。
- 不修改文案、字号、边框、卡片高度或响应式列数。

## 验收标准

- 同一行四个说明文字的顶部和底部坐标完全一致。
- 中英文及波斯语共用相同布局规则。
- 页面继续通过现有测试和生产构建。
